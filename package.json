{
  "name": "@example-apps/fleet",
  "private": true,
  "packageManager": "bun@1.3.12",
  "scripts": {
    "test": "bash scripts/run-expo-apps.sh test && bun test pages/src",
    "typecheck": "bash scripts/run-expo-apps.sh typecheck",
    "build": "bash scripts/run-expo-apps.sh build",
    "verify": "bun run test && bun run typecheck && bun run build",
    "build:pages": "bash scripts/build-pages.sh",
    "verify:pages": "bun test pages/src && bun run build:pages"
  }
}
